import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
    logoContainer: {},
    image: {},
    grow: {
        flexGrow: 1,
    },
    navOpenToggle: {
        cursor: "pointer",
    },
    drawer: {},
    navCloseToggle: {},
}));
