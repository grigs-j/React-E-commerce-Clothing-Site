import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
    accentBar: {
        backgroundColor: "#1990c6",
        color: "white",
        height: "50px",
        marginTop: "55px",
        paddingTop: "16px",
    },
}));
